{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" } // change to "build" for CRA
    }
  ],
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
